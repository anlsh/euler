(defun solve76 ()
  ;; TODO Actually implement a solution for this. The number of paritions
  ;; of 100 was literally given to us in class as a throwaway example, I just
  ;; subtracted 1 from it (take out the parition consisting of a single number,
  ;; the number itself)
  (- 190569292 1))
